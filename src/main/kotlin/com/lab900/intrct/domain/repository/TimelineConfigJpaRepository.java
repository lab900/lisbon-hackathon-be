package com.lab900.intrct.domain.repository;

import com.lab900.intrct.domain.TimelineConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimelineConfigJpaRepository extends JpaRepository<TimelineConfig, Long> {
}
